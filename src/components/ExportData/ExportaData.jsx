import React from "react"
import { Button } from "react-bootstrap"

const ExportData = () => {
    return (
        <div>
            <Button variant="success" style={{width: "100%", border: "none"}}>Export data to table</Button>
        </div>
    )
}

export default ExportData